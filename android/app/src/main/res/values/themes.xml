<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Everything visible is drawn by Compose. This theme exists only to decide what the window
         looks like in the moment before the first frame lands, so it carries a background and
         nothing else. No action bar: the page has no title, by design. -->
    <style name="Theme.LegionControl" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
    </style>
</resources>
