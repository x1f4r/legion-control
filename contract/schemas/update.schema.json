{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "update.schema.json",
  "title": "legionctl update",
  "description": "One service's manual update. `from` and `to` are the versions actually read on the machine, never the target that was aimed at.",
  "type": "object",
  "allOf": [
    {
      "$ref": "common.schema.json#/$defs/envelope"
    },
    {
      "$ref": "mutation.schema.json#/$defs/conflictRule"
    },
    {
      "$ref": "mutation.schema.json#/$defs/queueRule"
    },
    {
      "$ref": "mutation.schema.json#/$defs/acceptedRule"
    }
  ],
  "properties": {
    "action": {
      "enum": [
        "updated",
        "noop",
        "deferred",
        "queued",
        "failed",
        "rolled-back",
        "accepted",
        "conflict",
        "interrupted",
        "cancelled",
        "expired",
        "already-running"
      ]
    },
    "service": {
      "$ref": "common.schema.json#/$defs/configuredId"
    },
    "from": {
      "$ref": "common.schema.json#/$defs/version"
    },
    "to": {
      "$ref": "common.schema.json#/$defs/version"
    },
    "op": {
      "$ref": "mutation.schema.json#/$defs/opOrNull"
    },
    "detached": {
      "type": "boolean"
    },
    "replayed": {
      "type": "boolean"
    },
    "replaced": {
      "$ref": "common.schema.json#/$defs/opIdOrNull"
    },
    "conflict": {
      "$ref": "common.schema.json#/$defs/opConflict"
    },
    "verified": {
      "type": "boolean"
    },
    "opId": {
      "description": "The operation id, echoed beside the record. Additive: the canonical place to read it is op.id.",
      "$ref": "common.schema.json#/$defs/opId"
    }
  },
  "required": [
    "action",
    "service",
    "from",
    "to",
    "op"
  ],
  "unevaluatedProperties": false
}
