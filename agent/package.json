{
  "name": "legionctl",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Legion Control agent: service lifecycle, busy detection, durable operations, boot switching and sleep for one system",
  "main": "src/index.mjs",
  "bin": {
    "legionctl": "src/index.mjs"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "check": "node scripts/check-syntax.mjs",
    "test": "node --test test/*.test.mjs",
    "status": "node src/index.mjs status"
  },
  "dependencies": {}
}
